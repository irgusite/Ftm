ftm_moderate_player:
    pattern: /moderate/accept/{uniqid}
    defaults: { _controller: FtmPlayerBundle:Moderation:accept }
    requirements:
        uniqid: ^[a-z0-9]{32}

ftm_inscription:
    pattern: /inscription
    defaults: { _controller: FtmPlayerBundle:Player:inscription }